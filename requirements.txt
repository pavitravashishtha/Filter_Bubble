# Filter Bubble Simulation — Python dependencies
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy>=2.5.1
pandas>=3.0.3
networkx>=3.6.1
matplotlib>=3.11.1

# Web dashboard
streamlit>=1.59.2

# LLM integration (optional — only needed when use_llm=True)
groq>=1.5.0

# Testing
pytest>=9.1.1
